import React from "react";
import { render, fireEvent, screen } from "@testing-library/react";
import App from "../Menu/index";

describe("Menu Component", () => {
  test("Panel is not shown when webpage is loaded", () => {});
});

describe("Toggling Menu Modal", () => {
  test("Panel should appear when menu icon is clicked", () => {});

  test("Panel should disappear when menu icon is clicked again", () => {});
});
