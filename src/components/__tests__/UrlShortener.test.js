import React from "react";
import { render, fireEvent, screen } from "@testing-library/react";
import UrlShortener from "../UrlShortener/index";

describe("UrlShortener Component", () => {
  test("Contains text when user types in input field", () => {});

  test("Should not render short links if nothing is typed in input field", () => {});

  test("Should render without any data", () => {});

  test("", () => {});

  test("Should add short link card", () => {});

  test("Should remove last element when 5 short links are made", () => {});
});
